1️⃣ Create `.env` → add backend LoadBalancer URL  
2️⃣ Build & push frontend Docker image → Docker Hub  
3️⃣ Update `frontend.yaml` → set new image  
4️⃣ Apply `frontend.yaml` + `frontend-svc.yaml`  
5️⃣ Get frontend LoadBalancer URL → open in browser 
